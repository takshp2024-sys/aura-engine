import React,{useEffect,useState}from'react';import{onAuthStateChanged,signInAnonymously,signInWithPopup,GoogleAuthProvider,signOut}from'firebase/auth';import{auth}from'./firebase';import{getLocalWeather}from'./services/weather';import{CATEGORIES}from'./constants';
export default function App(){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);const[weather,setWeather]=useState(null);const[weatherLoading,setWeatherLoading]=useState(false);useEffect(()=>onAuthStateChanged(auth,u=>{setUser(u);setLoading(false)}),[]);
const guest=async()=>{try{await signInAnonymously(auth)}catch(e){alert(e.message)}};const google=async()=>{try{await signInWithPopup(auth,new GoogleAuthProvider())}catch(e){alert(e.message)}};const weatherNow=async()=>{setWeatherLoading(true);try{setWeather(await getLocalWeather())}catch(e){alert(e.message)}finally{setWeatherLoading(false)}};
if(loading)return <main className="center"><div className="card"><h1>AURA ENGINE</h1><p>Loading your styling engine…</p></div></main>;
if(!user)return <main className="center"><div className="card hero"><div className="logo">✦</div><h1>AURA ENGINE</h1><p>Your AI-powered personal wardrobe and styling assistant.</p><div className="actions"><button onClick={google}>Continue with Google</button><button className="secondary" onClick={guest}>Continue as Guest</button></div></div></main>;
return <main className="shell"><header><div><span className="eyebrow">AURA ENGINE 2.0</span><h1>Your wardrobe, smarter.</h1></div><button className="secondary small" onClick={()=>signOut(auth)}>Sign out</button></header><section className="grid"><div className="card"><h2>Wardrobe</h2><p>Real wardrobe photos, cloud storage, AI tagging and lookbook features are being built on this protected upgrade branch.</p><div className="chips">{CATEGORIES.map(c=><span key={c.id}>{c.icon} {c.label}</span>)}</div></div><div className="card"><h2>Weather styling</h2><p>{weather?weather.description:'Use your location to get current weather for styling recommendations.'}</p><button onClick={weatherNow} disabled={weatherLoading}>{weatherLoading?'Getting weather…':'Get current weather'}</button></div></section></main>}