import React,{useEffect,useState}from'react';import{onAuthStateChanged,signOut}from'firebase/auth';import{auth}from'./firebase';import Auth from'./components/Auth';import Wardrobe from'./components/Wardrobe';
export default function App(){const[user,setUser]=useState(undefined);useEffect(()=>onAuthStateChanged(auth,setUser),[]);if(user===undefined)return <main className="center"><div className="card"><h1>AURA ENGINE</h1><p>Loading your styling engine…</p></div></main>;if(!user)return <main className="center"><div className="card hero"><div className="logo">✦</div><h1>AURA ENGINE</h1><p>Your AI-powered personal wardrobe and styling assistant.</p><Auth/></div></main>;return <main className="shell"><header><div><span className="eyebrow">AURA ENGINE 2.0</span><h1>Your wardrobe, smarter.</h1></div><button className="secondary small" onClick={()=>signOut(auth)}>Sign out</button></header><Wardrobe userId={user.uid}/></main>}