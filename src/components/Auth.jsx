import React,{useState}from'react';import{GoogleAuthProvider,signInAnonymously,signInWithPopup}from'firebase/auth';import{auth}from'../firebase';export default function Auth(){const[busy,setBusy]=useState(false);const run=async fn=>{setBusy(true);try{await fn()}catch(e){alert(e.message)}finally{setBusy(false)}};return <div className="actions"><button disabled={busy} onClick={()=>run(()=>signInWithPopup(auth,new GoogleAuthProvider()))}>Continue with Google</button><button disabled={busy} className="secondary" onClick={()=>run(()=>signInAnonymously(auth))}>Continue as Guest</button></div>}